# 午餐選困互助會 — 更新紀錄

> 給 Claude 讀的：每次改版都往上加一節。記「改了什麼、為什麼、動到哪個檔案的哪一段」，
> 不要只寫「修 bug」。程式碼在 `app.html`（樣板）→ `build.py` 注入資源 → 產出 `index.html`。
> 像素素材在 `/home/claude/px/`：`art2.py`（食物圖鑑+關鍵字）、`faces.py`（32×32 頭像）、
> `ui.py`（UI 圖示）、`scene2.py`（背景場景）。

---

## v1.0.3 — 2026-08-11

**口袋名單可以改圖示**

原本店家的圖示只有新增當下靠關鍵字自動判斷，猜錯就再也改不掉（記帳表單的徽章只會改
「這一筆」，不會回寫店家）。

`app.html` 改動：
- 圖鑑彈窗 `#mIcons` 從「只服務記帳表單」改成可指定對象。新增 `iconTarget` 變數與
  `openIcons(shop)`：傳店家物件就寫回 `shops`，傳 `null` 就是原本的記帳表單行為。
- 彈窗標題 `#iconTitle` 會顯示正在改哪一家，避免搞混。
- 口袋名單的圖示改成 `<button>`，點了開圖鑑。列表下方統一放一行提示，不要每列都寫。
- `paint()` 裡原本會去改圖鑑按鈕的選取狀態，移掉了——選取狀態現在由 `openIcons()`
  依當下對象決定，不然改完店家再開表單會顯示錯的高亮。

改圖示只影響店家本身和之後新記的帳；已經記過的紀錄保留當時的圖示，不會被追溯修改。

---

## v1.3 — 2026-08-12

**遭遇改成 JRPG 戰鬥畫面（可切回舊版）**

「遭遇」分頁上方換成一個 392px 的戰鬥舞台：背景是像素場景、上方是放大的店家圖示當敵人、
下方是三個角色的背影。抽籤 = 遇敵演出，「再抽一次」改叫「逃跑」。

**使用者擔心之後想換回去，所以舊版一定要保留。做了兩層備份：**
1. `index-v1.2-classic.html`（純檔案備份，可直接覆蓋回去）
2. **App 內建切換**：設定 → 遭遇的呈現方式 → 戰鬥畫面 / 經典（純文字）。
   兩套程式碼並存，`S.cfg.enc` 存偏好，`applyEncStyle()` 控制顯示。

**戰鬥素材**（`px/sback.py`，40×44、每人 3 格：待機 A / 待機 B / 攻擊）

畫這組來回改了七八輪，把踩過的坑記下來：

- **四分之三側面在小畫布畫不出來。** 試過 22×28 的側面視角，脖子和上下半身會錯位、
  戰士的身體被劍吃掉一塊。**改成完全背對視角反而正確**——敵人在畫面上方，
  背面視角本來就是對的，而且對稱身體不會變形。
- **手臂要讓人看得出是肢體，需要三件事**：(a) 手臂上下各一排外框；
  (b) 袖子用衣服的**暗色**（同色會糊成一團）；(c) 袖子和軀幹之間保留軀幹自己的側邊外框。
- **袖子和手之間不能有外框**。多那一格會變成一顆黑像素，看起來像手被切斷。
- **武器要「握」住**：先畫武器，再把手的中間兩列補畫回最上層。這樣武器蓋住大部分的手，
  只露出握著的一小段，比整隻手露出來自然得多。
- **28 格寬還是不夠**，武器只剩 2-3px 像牙籤。**40 格才畫得下**反曲弓、鳶形盾、
  十字護手劍、法杖。
- 弓用正弦曲線生成，**握把（弧線中段）要朝向手**；方向畫反會變成鐮刀。
  弓弦要亮色 1px，用灰的會糊掉。
- **長髮從背後看中間不可能露出脖子** → 改綁馬尾（髮圈 + 中線髮束）。
  另外**不要為了長髮把頭部加寬**，那會讓頭左右凸出。
- 明暗一律光源左上：衣服左亮右暗、頭髮左高光右暗部、木頭與金屬各兩階。

**特效全部是像素圖**（不是 CSS 漸層／圓角）：
- Yulun：箭矢往**畫面上方**飛（敵人在前方，不是左邊），命中後爆白色星芒
- Eric：斜向彎月刀光，白芯 + 淡藍外緣 + 電光，用圓弧公式生成
- Karen：青色魔法符文環
- 影子也是像素橢圓

**踩到的 JS 坑**：`Object.assign(el.style, {'--dy': ...})` **設不了 CSS 自訂屬性**，
必須用 `el.style.setProperty()`。這個 bug 讓箭的位移一直是 undefined，卡在原地不動。

**流程**：抽籤 → 逃跑重抽（會套用冷卻天數）→「就這家」播三段攻擊 → 自動跳到記錄頁
並帶入店名和圖示。空名單時不會進戰鬥。

---

## v1.2 — 2026-08-12

**圖鑑大改：分類重組 + 六顆重畫 + 關鍵字全面校對**

分類異動（使用者定案，全部有理由）：

| 動作 | 原因 |
|---|---|
| 三明治 → **滷味** | 台灣午餐吃三明治遠少於滷味 |
| 烤雞 + 雞排 → 先合併，後拆開 | 使用者指出**雞排跟鹹酥雞是同一攤**，所以雞排歸 `friedchicken`，`chickenmeat` 只留整隻雞（烤雞/甕仔雞/油雞/白斬雞/火雞肉飯/烤鴨），標籤「烤雞」 |
| 火鍋 → **大餐**（`feast`） | 要一個概括「聚餐、慶祝、比較貴的一餐」的類別；火鍋/牛排/燒烤/吃到飽全部收進來 |
| 牛肉麵 → 併入**台式麵店** | 本來就是麵店賣的。順手在麵碗圖裡加一塊肉，讓陽春麵和牛肉麵都說得通 |
| 新增 **熱炒**（`wok`） | 炒飯/炒麵/快炒/燴飯/羹原本**完全沒有對應**，是最大的漏洞 |

最終 24 個。使用者確認不需要素食（沒人吃素），所以「素食/蔬食」目前仍是已知缺口，
之後要加得犧牲一格。

**舊 key 的相容處理**

`hotpot` / `beefnoodle` / `roastchicken` / `sandwich` 這四個 key 已經不存在，但使用者
手機和 Sheet 裡可能已經有用到它們的紀錄。前端加了 `ALIAS` 對照表 + `ic()` 正規化函式，
**在讀取時轉換，不改資料**——這樣不用寫回 Sheet、不會產生同步流量、三支手機各自處理。
`pxc()`、遭遇畫面的 `draw()`、編輯表單、店家編輯、`guess()` 回傳都要走 `ic()`，
少一處舊紀錄就會顯示成便當。

**關鍵字校對（找到三個真的會誤判的）**

`GUESS` 是按物件順序比對，**先命中的贏**，所以順序即優先序：

| 症狀 | 原因 | 修法 |
|---|---|---|
| 生魚片 → 丼飯 | `donburi` 有「生魚」且排在 `sushi` 前面 | 改成「鮭魚丼」 |
| 便當店 → 自助餐 | `buffet` 關鍵字含「便當店」 | 移除，讓它落到 `bento` |
| 茶餐廳 → 手搖飲 | `boba` 有裸字「茶」，太廣 | 改成紅茶/綠茶/珍奶 |

另補大量漏字：肯德基、統一超商、弘爺、拉亞、麥味登、八方雲集、粄條、麵線、涼麵、
焢肉、悟饕、迷客夏、大苑子、燉飯、披薩、鐵板麵、蘿蔔糕…
45 個真實店名測試，只有「某某小館」「阿姨的店」「老王」猜不出來——**那是正確行為**。

**圖示品質（六顆重畫）**

| 圖示 | 原本的問題 |
|---|---|
| 水餃 | 兩坨麵團看不出是餃子 → 加摺痕、放到盤子上 |
| 鍋貼 | 三條橫的像金條 → 立起來、底部焦黃 |
| 壽司 | 三層堆疊像漢堡 → 改握壽司（飯＋鮭魚） |
| 咖哩 | 第一版像麵包，第二版像咖啡杯 → 左白飯右咖哩的盤子 |
| 燒臘 | 白盤看不出東西 → 紅色叉燒切片＋青菜 |
| 手搖飲 | 跟咖啡太像 → 加吸管＋杯底珍珠 |

新畫的三顆也各試了三四版才成立：
- **雞腿**：對稱的看起來像獎盃，骨頭太細像棒棒糖。要**肉塊偏左上、骨頭斜 45 度、末端兩瓣骨節**。
- **大餐**：牛排盤在 16 格畫出來像電視，換成**銀色餐蓋**剪影才乾淨。
- **熱炒**：鐵鍋＋長柄＋翻起來的料＋底下的火。

**流程檢討**：使用者這輪明確要求「先給圖再改 index」。前一版我畫完直接就更新了，
造成他要退回。**美術類改動一律先出審查圖，確認後才動 `index.html`。**

---

## v1.1.2 — 2026-08-12

**齒輪重畫（第二次）**

v1.1.1 那顆使用者退回，理由：單色、只有描邊沒有量感，不像齒輪。他給了一張 RPG 風格的
青銅齒輪參考圖。**16×16 塞不下參考圖的三個特徵**（青銅明暗層次、金色內環、梯形齒的塊面），
所以改成 **32×32**，顯示 32px 1:1，跟旁邊的拉麵 crest 同一個像素密度。
檔案獨立成 `px/gear32.py`，程式化產生（`GEAR` / `PAL`）。

幾何參數（改動時整組要一起調，比例錯了就不像齒輪）：
```
齒尖 15.3 / 環外緣 11.8 / 金環 8.6–5.4 / 中孔 5.4   （中心 15.5）
齒寬 half = 3.0 - 0.4*t   （t 是從環外緣到齒尖的比例，外窄內寬）
```
- **齒必須用「旋轉座標系裡的固定像素寬」定義，不能用角度寬度。**
  用角度的話外圈會愈來愈細，變成太陽光芒不是齒輪。
- 8 顆齒在半徑 11.8 的圓周上間距約 9.3px，**齒寬要壓在 6px 以內**才有可見的齒谷；
  超過就跟本體融成一團。
- 環太薄（<3px）看起來像個圈不像齒輪，參考圖的環厚度約佔半徑的三分之一。
- 光源左上，青銅四階 + 金三階，共 8 色。

**圖鑑：三明治 → 滷味**

使用者要求。台灣午餐吃三明治的頻率遠低於滷味。`art2.py` 裡 `sandwich` 整組換成 `luwei`
（圖、調色盤、`LABEL`、`GUESS` 關鍵字）。關鍵字涵蓋：滷味、滷、關東煮、黑白切、甜不辣、
鹽水雞。實測「阿婆滷味」「關東煮」「鹽水雞」「甜不辣」都判斷正確。

構圖：淺色紙碗 + 深褐色滷料塊面（豆干/海帶用不同棕階區分）+ 兩根竹籤斜插 + 一點青菜。
第一版把碗畫太高、滷料糊成一團看不出東西，加了色階變化和竹籤才讀得出來。

**注意**：圖鑑總數仍是 24，沒有增加。已經記過三明治的舊紀錄（如果有）圖示會 fallback
到便當，因為 `pxc()` 找不到 key 時會退回 `FOOD.bento`。

---

## v1.1.1 — 2026-08-12

**右上設定齒輪改像素風**

漏網之魚，那顆一直是文字 `⚙`（系統字），跟其他 UI 圖示不同調。加了 `ui.py` 的 `gear`，
16×16、以 32px 顯示。

畫 16px 齒輪的心得（重畫了六次）：
- **齒一定要跟本體有明顯落差才讀得出來。** 本體半徑 4.6 + 8 顆 2×2 的齒塊。
  本體只要放大到 5.2，對角的齒就會被圓形吃掉，整個看起來像菱形不像齒輪。
- 4 顆齒不夠，看起來像十字或菱形。8 顆才有齒輪感。
- 中孔用半徑 2.1 的圓，太大環會全部變成描邊沒有填色，太小會看起來像一個點。
- 用程式算幾何比手畫可靠，手畫在 16px 下很難對稱。

---

## v1.1 — 2026-08-11

實際用了半天之後回報的三個問題。

**1. 口袋名單可以編輯了**

原本只能刪不能改，系統猜不到的店名（「阿姨的店」這種）永遠卡在便當圖。
現在整列可點，開「編輯店家」彈窗，可以改名字和圖示。

**改圖示或改名會連動更新該店已記過的紀錄**（使用者指定）。`entries` 存的是當下的 icon
快照，不連動的話日誌會同一家店出現兩種圖。用 `putMany` 一次寫，避免 render 很多次。

踩到的坑：圖鑑彈窗從店家編輯打開時會被蓋住。三個 `.mask` 都是 `z-index:60`，
DOM 順序 mIcons → mShop → mDay，所以後面的畫在上面。**`#mIcons` 拉到 `z-index:70`**。
另外關閉圖鑑時只有 mIcons 要重置 `iconTarget`，不能順手關掉底下的 mShop。

圖鑑彈窗現在服務兩個地方，用 `iconTarget`（`'entry'` / `'shop'`）區分，
入口統一走 `openIcons(where, now)`。

**2. 口袋名單收束**

累積到 20–30 家會把「遭遇」頁拖很長。預設只顯示 5 家（照「最久沒吃」排，剛好是最想吃的），
下面「還有 N 家 ▼」展開。新增輸入框留在原地，那是高頻動作。

**3. 分類（午餐 / 點心 / 晚餐）+ 個人統計**

問題根源：使用者有時候只有兩個人去吃晚餐、下午訂飲料，而統計把
「每人平均」寫死成 `總額 ÷ 3`，數字會失真。**`a1/a2/a3` 本來就分開存，資料是準的，
是我沒拿來用。** 這是真的算錯，不只是分類問題。

- `entries` 新增 `meal` 欄位（`lunch` / `snack` / `dinner`），舊資料一律當 lunch。
- **原本想用時間自動判斷（11–15 點=午餐…），使用者否決**：他們常常事後補登，
  時間判斷只會一直猜錯，每次都要改回來反而更煩。改成純按鈕、預設停在午餐。
  午餐本來就佔九成 → 九成情況零操作，而且不會有「猜錯還要改」的反效果。
- 統計頁最上面加「全隊 / Yulun / Eric / Karen」切換，**欄位數量不變、只換內容**，
  這樣不會愈加愈長。個人模式下不除以 3，且只算他真的有吃的那幾餐
  （`joined()` 過濾 `a[i] > 0`）。
- 統計主體只算午餐，點心/晚餐在數字卡最下面用一行 `另有　點心 6 次 480 元` 帶過。
- 月曆非午餐的格子右下角加金色小點。日誌列非午餐顯示 `［點心］` 標記。
- 分帳/清算**完全不分類**，全部一起算。分開結反而麻煩。

**Sheet 欄位變更 + 自動升級**

`entries` 多了 `meal`（插在 `icon` 之後）。`Code.gs` 加了 `migrate_()`：
偵測到表頭跟預期不符時，照**欄名**把每一列搬到新位置，新欄補空值。
所以使用者不用手動改 Sheet，但**一定要重新部署 Code.gs**才會生效。

**其他修正**
- 選完「你是誰」之後沒有重建表單，記錄頁的「記錄者」會一直顯示 `?`。
  `whoGo` 補上 `buildForm()` + `resetForm()`。

---

## v1.0.2 — 2026-08-11

**換 App 圖示：拉麵**

舊的金色碗+筷子（`ui.py` 的 `crest`）只有 16×16 又是單色金，在手機桌面上看不出是什麼。
重畫成 32×32、22 色的拉麵，檔案在 `px/ramen.py`。提了三案給使用者挑：
A 正面碗、B 筷子夾麵、C 俯視碗。**選 B，並要求蒸氣加多**。

B 定稿的重點（之後要改記得維持這幾點）：
- **筷子一定要兩支**。第一版用一條 3px 斜線，在 60px 下就是一根棍子。現在是兩組 2px
  斜線中間留 2px 空隙。
- 蒸氣三道，用交錯鋸齒讓它有飄動感，不要三條直線。
- 從筷子垂下的麵條只能 1–2px 波浪。第一版畫成實心柱狀，糊成一團看不出是麵。
- 右側原本整片都是麵，顏色太空，補了魚板和蔥花。

用在兩個地方：`apple-touch-icon`（`build.py` 的 `RM.appicon()` 合成 180px 深藍圓角底 +
金框）、標題列 crest（32×32 原尺寸，1:1 不縮放）。JS 端變數是 `RAMEN` / `RPAL`。

**月曆箭頭改像素風**

原本用文字 `◀ ▶`。改成 16×16 手繪像素箭頭（`ui.py` 的 `arrowL` / `arrowR`），
以 32px 顯示（2 倍，整數倍才不會糊）。

---

## v1.0.1 — 2026-08-11

**修：iOS 安全區域露出底色，各分頁選單位置不一致**

症狀：真機上「隊伍」分頁（內容短、不用捲）選單下方會露出一條淺藍色，且選單位置比其他
分頁高。原因是 `viewport-fit=cover` 在部分情況沒生效，版面視口底部在 home indicator 之上，
下方區域顯示的是 canvas 背景色。

`app.html` 改動：
- `html{background:#0f1530}` — 只設在 html，**不能同時設在 body**。body 有自己的背景時
  會蓋掉 `#bg`（負 z-index 的子元素畫在 root 背景之上、但在 body 背景之下）。第一次改的
  時候踩到這個，整張背景場景消失。
- `nav::after` 往下鋪 160px 的 `#0b0e1c`，保證選單下方永遠是深色。
- `body` 和 `main` 加 `min-height`，用 `dvh`（`vh` 當退路），讓長短頁面的選單位置一致。
- `#bg` 從 `inset:0` 改成 `top/left/right:0 + height:100dvh`。

---

## v1.0 — 2026-08-11 首版上線

**架構**
- 前端：單一 `index.html`（約 616 KB），字型、場景、所有像素圖都用 base64 內嵌，無外部相依。
  放在 GitHub Pages `yulunyeh0731/lunch-guild`。
- 後端：Google Apps Script 綁定在試算表「午餐選困互助會 — 帳本」
  （fileId `148CEVfN3jLCSWnZhPVIwVyxW8-Rr_mjPF_d5e2BZ_No`），對外只有 `doGet` / `doPost` 兩支。
- 同步：逐列 upsert，同 id 比 `updatedAt` 大的贏。刪除打墓碑（`deleted=1`）不真的刪列。
  離線寫入排進 `S.outbox`，連上自動送出。

**Sheet 欄位**
```
members : id, name, portrait, cls, job, updatedAt, deleted
shops   : id, name, icon, by, updatedAt, deleted
entries : id, date, shop, icon, payer, a1, a2, a3, note, by, periodId, updatedAt, deleted
periods : id, closedOn, fromDate, toDate, transfers, by, updatedAt, deleted
```
`a1/a2/a3` 對應 members 的前三列（m1/m2/m3），順序固定。

**踩過的坑（別再犯）**
- Apps Script 不回應 OPTIONS 預檢，POST 一定要用 `Content-Type: text/plain;charset=utf-8`。
- 同步進行中排隊的變更會被 `S.outbox = []` 清掉。關帳要一次寫 6 筆會觸發，資料會消失。
  已改成只清「這一批實際送出的」，並在結束後檢查 outbox 是否還有東西、有的話再送一輪。
- 像素圖縮放必須是整數倍（16→32、24→48），非整數倍會糊。
- Cubic 11 是 11×11 點陣字，字級只能用 11 的倍數（11/22/33px）。

**部署設定**
- Apps Script 部署：執行身分「我」、存取權「任何人」。選「擁有 Google 帳戶的任何使用者」
  一樣會失敗，因為 `fetch()` 跨網域是匿名請求。
- 改了 `Code.gs` 要「管理部署作業 → 編輯 → 版本選新版本 → 部署」，存檔不會生效。

**除錯備忘**
- Claude 的 web_fetch 打 Apps Script `/exec` 一律回 404（推測是沒跟隨 302 到
  googleusercontent）。**這是假警報，不要當成部署有問題的證據。**
  要驗證後端請改用：讀試算表看分頁/資料有沒有出現，或請使用者用無痕視窗開網址。
